var simpleList, simpleObject, simpleVar;

simpleVar = 'variable 1';

console.log(simpleVar, 'var1');

simpleList = ['alpha', 'beta', 'gamma', 'delta'];

console.info(simpleList, 'var2', false);

simpleObject = {
  partA: 'A',
  partB: 'B',
  partC: 'C',
  partD: 'D'
};

console.log(simpleObject, 'var3');

console.all();
